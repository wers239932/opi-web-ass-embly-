<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <!-- Исходные файлы --># Обновите пути к исходникам
    src.dir=../web3/src/main/java
    resources.dir=../web3/src/main/resources
    <entry key="webapp.dir">src/main/webapp</entry>

    <!-- Выходные директории -->
    <entry key="build.dir">build</entry>
    <entry key="classes.dir">${build.dir}/classes</entry>
    <entry key="jar.dir">${build.dir}/jar</entry>
    <entry key="test.dir">src/test/java</entry>
    <entry key="test.reports.dir">${build.dir}/test-reports</entry>

    <!-- Имя JAR-файла -->
    <entry key="jar.name">web3-app.jar</entry>

    <!-- Главный класс для MANIFEST.MF -->
    <entry key="main.class">beans.Service</entry>

    <!-- Версия проекта -->
    <entry key="version">1.0</entry>
</properties>
